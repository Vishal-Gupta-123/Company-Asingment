/**
 * Scroll-Driven Hero Section Animation
 * Uses vanilla JavaScript with requestAnimationFrame for smooth performance
 */

// ===== Configuration =====
const CONFIG = {
    // Animation durations (in milliseconds)
    letterAnimationDuration: 1200,
    letterStaggerDelay: 40,
    statAnimationDuration: 800,
    statStaggerDelay: 150,
    
    // Scroll animation settings
    scrollSensitivity: 0.5,
    parallaxStrength: 0.3,
    
    // Number counter settings
    counterDuration: 2000,
    counterFPS: 60,
};

// ===== State Management =====
const state = {
    scrollY: 0,
    windowHeight: window.innerHeight,
    isLoaded: false,
    animations: }
        lettersComplete: false,
        statsComplete.
        